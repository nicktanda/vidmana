<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Vidmana" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <style>
      <%= File.read(Rails.root.join('app/assets/stylesheets/tailwind.css')).html_safe %>
    </style>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-[#212121] text-gray-100 m-0 p-0 overflow-hidden">
    <% if user_signed_in? %>
      <!-- ChatGPT-like layout with sidebar -->
      <div class="flex h-screen w-screen">
        <!-- Sidebar -->
        <div class="w-64 bg-[#171717] border-r border-gray-700 flex flex-col flex-shrink-0">
          <!-- Logo/Brand -->
          <div class="p-4 border-b border-gray-800">
            <%= link_to authenticated_root_path, class: "flex items-center space-x-2 hover:opacity-80 transition-opacity" do %>
              <span class="text-xl font-bold text-black">Vidmana</span>
            <% end %>
          </div>

          <!-- New Universe Button -->
          <div class="p-3">
            <%= link_to authenticated_root_path, class: "flex items-center space-x-2 px-3 py-2 rounded-lg border border-gray-600 hover:bg-[#2a2a2a] transition-colors text-gray-200" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
              </svg>
              <span>New Universe</span>
            <% end %>
          </div>

          <!-- Navigation -->
          <nav class="flex-1 overflow-y-auto p-3 space-y-1">
            <% owned = current_user.universes.order(created_at: :desc).limit(10) %>
            <% shared = current_user.shared_universes.order(created_at: :desc).limit(10) %>
            <% if owned.any? || shared.any? %>
              <div class="mb-2">
                <div class="px-3 py-2 text-xs font-semibold text-gray-400 uppercase tracking-wider">
                  Universes
                </div>
                <% owned.each do |universe| %>
                  <%= link_to universe, class: "block px-3 py-2 rounded-lg hover:bg-[#2a2a2a] transition-colors text-gray-300 hover:text-white text-sm truncate" do %>
                    <%= universe.name %>
                  <% end %>
                <% end %>
                <% if shared.any? %>
                  <div class="px-3 py-2 text-xs font-semibold text-gray-400 uppercase tracking-wider mt-3">
                    Shared With Me
                  </div>
                  <% shared.each do |universe| %>
                    <%= link_to universe, class: "flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-[#2a2a2a] transition-colors text-gray-300 hover:text-white text-sm" do %>
                      <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/>
                      </svg>
                      <span class="truncate"><%= universe.name %></span>
                    <% end %>
                  <% end %>
                <% end %>
              </div>
            <% end %>

            <!-- Tools Section -->
            <div class="mb-2">
              <div class="px-3 py-2 text-xs font-semibold text-gray-400 uppercase tracking-wider">
                Tools
              </div>
              <%= link_to mana_prompts_path, class: "flex items-center space-x-2 px-3 py-2 rounded-lg hover:bg-[#2a2a2a] transition-colors text-gray-300 hover:text-white text-sm" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                </svg>
                <span>ManaPrompt</span>
              <% end %>
            </div>
          </nav>

          <!-- User Profile -->
          <div class="p-4 border-t border-gray-800">
            <div class="flex items-center justify-between">
              <div class="flex items-center space-x-3">
                <% if current_user.avatar_url.present? %>
                  <img src="<%= current_user.avatar_url %>" alt="<%= current_user.name %>" class="h-8 w-8 rounded-full">
                <% else %>
                  <div class="h-8 w-8 rounded-full bg-purple-600 flex items-center justify-center">
                    <span class="text-white text-sm font-medium">
                      <%= current_user.name.first.upcase %>
                    </span>
                  </div>
                <% end %>
                <span class="text-sm"><%= current_user.name %></span>
              </div>
              <%= button_to destroy_user_session_path, method: :delete, data: { turbo: false },
                          class: "text-gray-400 hover:text-white" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                </svg>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Main Content -->
        <div style="flex: 1 1 0%; display: flex; flex-direction: column; overflow: hidden; min-height: 0;">
          <!-- Notifications -->
          <% if notice.present? %>
            <div class="flex-shrink-0 bg-green-900/50 border-b border-green-800 text-green-200 px-6 py-3">
              <%= notice %>
            </div>
          <% end %>
          <% if alert.present? %>
            <div class="flex-shrink-0 bg-red-900/50 border-b border-red-800 text-red-200 px-6 py-3">
              <%= alert %>
            </div>
          <% end %>

          <!-- Scrollable Content -->
          <main style="flex: 1 1 0%; display: flex; flex-direction: column; overflow: hidden; min-height: 0;">
            <%= yield %>
          </main>
        </div>
      </div>
    <% else %>
      <!-- Non-authenticated layout -->
      <% if notice.present? %>
        <div class="bg-green-900/50 border border-green-800 text-green-200 px-4 py-3 rounded mx-4 mt-4">
          <%= notice %>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="bg-red-900/50 border border-red-800 text-red-200 px-4 py-3 rounded mx-4 mt-4">
          <%= alert %>
        </div>
      <% end %>

      <main>
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>